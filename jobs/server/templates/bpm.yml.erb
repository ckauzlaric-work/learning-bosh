<%=
require 'yaml'

bpm = {
  "processes" => [
    { "name" => "server",
      "executable" => "/var/vcap/packages/server/bin/server",
      "args" => [ "-c", "/var/vcap/jobs/server/config/server.yml"],
      "capabilities" => ["NET_BIND_SERVICE"]
    },
  ]
}

YAML.dump(bpm)
%>