---
name: server

templates:
  bpm.yml.erb: config/bpm.yml
  server.yml.erb: config/server.yml

packages:
- server

properties:
  port:
    description: "Listening Port for Server."
    default: 8080
